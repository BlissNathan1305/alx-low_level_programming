#include <stdio.h>

/**
 * main - main function
 *
 * Return: 0
 */


int main(void)
{
	int counter = 0;
	long int = 1;
	long int = a;
	long int = a + b;

	while (c = 4000000)
	{
		if (c % 2 == 0)
		{
			counter += c;
		}
		a = b;
		b = c;
		c = a + b;

	}
	printf("%d\n", counter);

	return (0);
}
